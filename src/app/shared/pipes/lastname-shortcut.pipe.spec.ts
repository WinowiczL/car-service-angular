import { LastnameShortcutPipe } from './lastname-shortcut.pipe';

describe('LastnameShortcutPipe', () => {
  it('create an instance', () => {
    const pipe = new LastnameShortcutPipe();
    expect(pipe).toBeTruthy();
  });
});
